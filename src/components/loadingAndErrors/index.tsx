import type { FC } from 'react';
import Header from '../Header';

export const LoadingAndErrors: FC = () => {

  return (
    <Header header="Loading & Errors">Some description about Loading &amp; Errors</Header>
  );
};

export default LoadingAndErrors;