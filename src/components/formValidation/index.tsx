import type { FC } from 'react';
import Header from '../Header';

export const FormValidation: FC = () => {

  return (
    <Header header="Form Validation">Some description about Form Validation</Header>
  );
};

export default FormValidation;